import { SxProps } from '@mui/material';

const filtersModalStyles: SxProps = {
  position: 'absolute',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%)',
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  rowGap: '1rem',
  maxWidth: 400,
  width: '100%',
  backgroundColor: '#fff',
  borderRadius: '10px',
  p: 2,
};

export default filtersModalStyles;
